# Dashboard Widget Reference

When rendering the stat command center widget, use `show_widget` with the full HTML from the PWA app at `C:\Users\user\Desktop\CLAUDE\07-REFERENCE\NXGENStatCenter.html`.

The PWA app is the canonical version — it includes:
- SheetJS for Excel export
- Google Sheets API integration
- Full stat entry, leaders, standings, audit log
- Service worker for offline use
- PWA manifest for desktop install
